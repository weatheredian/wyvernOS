name: build grgsm, push image
on:
  schedule:
    - cron: "10 0 1 * *"
  push:
    paths:
      - "files/grblocks/grgsm/Containerfile"
  workflow_dispatch:
concurrency:
  group: ${{ github.workflow }}-${{ github.ref || github.run_id }}
  cancel-in-progress: true
jobs:
  build-cache:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v5
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build & push grgsm layer
        uses: docker/build-push-action@v6
        with:
          context: files/grblocks/grgsm
          file: files/grblocks/grgsm/Containerfile
          push: true
          tags: ghcr.io/weatheredian/grgsm:${{ github.ref_name }}
